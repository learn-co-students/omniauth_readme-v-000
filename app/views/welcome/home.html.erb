  <% if session[:user_id] %>
    <h1><%= @user.name %></h1>
    <h2>Email: <%= %> </h2>
    <h2>Facebook UID: <%= %> </h2>
    <img src="<%= @user.image %>">
  <% else %>
    <%= link_to 'Login with Facebook!', '/auth/facebook'%>  
  <% end %>
